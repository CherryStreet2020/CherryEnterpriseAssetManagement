# .ship-config.md — CherryAI EAM project config for cowork-github-replit-process

```yaml
# --- Required ---
repo: CherryStreet2020/CherryEnterpriseAssetManagement
main_branch: main
mac_clone_path: /Users/deandunagan/Documents/Claude/Projects/EnterpriseAssetManagament/CherryEnterpriseAssetManagement
build_command: ~/.dotnet/dotnet build Abs.FixedAssets.csproj -nologo -clp:ErrorsOnly
build_success_match: Build succeeded
gh_path: /Users/deandunagan/bin/gh
replit_ide_url: https://replit.com/@DeanInNYC/FIxedAssetsProject-1-25-26-750pm
replit_live_url: https://e8802fe3-8508-4ead-883b-c5ea53b425aa-00-3qxqi4qcn5qjz.picard.replit.dev
replit_workspace_dir: /home/runner/workspace
framework: dotnet
memory_dir: /Users/deandunagan/Library/Application Support/Claude/local-agent-mode-sessions/77352526-fa9c-4057-a31c-3c6998c3695f/72dd441b-0e01-41a8-8253-0b4ef1b8a505/spaces/1bb0fceb-4fb8-4acf-86a2-a8f1b63b6472/memory

# --- Optional ---
design_system: ds-
auth_user: admin
auth_password_hint: demo accounts on the login page (admin/admin123 etc.)
local_build_skippable: false
cold_build_wait_seconds: 35

# --- Project-specific ---
csproj_main: Abs.FixedAssets.csproj         # main app — always build this
csproj_skip: Abs.FixedAssets.Tests.csproj   # test project has pre-existing errors; skip
master_plan_path: MASTER_PLAN.md             # source-of-truth for sprint progress
```

## Project-specific notes

### Route map (real URLs, not always what they're called in code)
- WO list lives at `/Maintenance` (NOT `/WorkOrders`)
- WO detail at `/WorkOrders/Details/{id}` (yes, inconsistent)
- Plant overview at `/Plant`, per-site at `/Plant/Floor/{id}`
- Assets list at `/Assets`, detail at `/Assets/Asset?id={id}` (query, not path)
- Approvals queue at `/Approvals/Pending`
- Reports: `/Reports/Reliability`, `/Reports/AssetReliability` (the spend report — NOT `/Reports/MaintenanceSpend` which was retired in PR #116a)
- Journals: `/Journals`, `/Journals/Details/{id}`
- Periods: `/Periods` (then `/Periods/Close` for the close packet)
- Purchasing: `/Purchasing`, `/Purchasing/Details/{id}`
- AP: `/AccountsPayable`, `/AccountsPayable/Details/{id}`
- Vendors: `/Materials/Vendors`
- Items: `/Materials/Items`
- Login: `/Account/Login`
- Design system showcase: `/Admin/DesignSystem`

### Design system primitives (drives E2E DOM checks)
- `.ds-card[data-tone="brand|success|warning|danger|info|neutral"]` — DataCard
- `.ds-kpi__value` / `.ds-kpi__delta` / `.ds-kpi__label` — KPITile
- `.ds-pill[data-tone="..."]` — StatusPill
- `.ds-btn` / `.ds-btn--primary` / `--secondary` / `--ghost` — Button
- `.ds-empty-state` — EmptyState v2
- `.ds-spark` — Sparkline
- `.ds-data-table` — DataTable (limited use — column filters/multi-sort not yet supported)

### Test posture
- Layer 1: DB schema validation bash scripts in `tests/db-validate-*.sh`
- Layer 2: integration scenarios in `tests/integration-*.sh`
- Layer 3: HTTP route smoke tests in `tests/route-smoke-*.sh`
- Layer 4: Playwright E2E (some specs age-flaky — check before relying)
- xUnit: `Abs.FixedAssets.Tests` has 9 skipped specs (intentional — rewrite backlog item)
```
